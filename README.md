# Jarvis-AI-Assistant
A voice-enabled AI chatbot using a pre-trained language model. It takes user input, remembers recent messages for context, generates responses, and displays everything in a web interface with text and speech output.
